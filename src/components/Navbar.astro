---
const navLinks = [
  { label: "Servicios", href: "#servicios" },
  { label: "Proceso", href: "#proceso" },
  { label: "Diferencial", href: "#diferencial" },
  { label: "Contacto", href: "#contacto" }
];
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-[#FAFAFA]/80 backdrop-blur-xl border-b border-black/[0.07]">
  <div class="max-w-7xl mx-auto px-6 md:px-12 py-4 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-2 group">
      <img src="/logo.webp" alt="Software Solutions Art" class="h-10 w-auto" />
      <span class="text-black/50 text-xs font-[family-name:var(--font-body)] font-light tracking-widest hidden md:inline">
        SOFTWARE SOLUTIONS
      </span>
    </a>

    <!-- Desktop Nav Links -->
    <div class="hidden md:flex items-center gap-8">
      {navLinks.map((link) => (
        <a 
          href={link.href}
          class="text-black/70 font-[family-name:var(--font-body)] font-light tracking-wide hover:text-[#E8002D] transition-colors duration-300"
        >
          {link.label}
        </a>
      ))}
    </div>

    <!-- CTA Button -->
    <a 
      href="#contacto"
      class="bg-[#E8002D] hover:bg-[#9B001E] text-white px-6 py-2 font-[family-name:var(--font-display)] font-bold uppercase tracking-wider transition-all active:scale-95"
    >
      Agendar llamada
    </a>
  </div>
</nav>